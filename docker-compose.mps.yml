services:
  api:
    # Note: Docker Desktop on macOS does not currently support direct GPU access.
    # This configuration documents MPS-related environment variables for reference,
    # but GPU acceleration is not available through Docker on Apple Silicon.
    environment:
      - PYTORCH_ENABLE_MPS_FALLBACK=1
      - PYTORCH_MPS_HIGH_WATERMARK_RATIO=0.0
